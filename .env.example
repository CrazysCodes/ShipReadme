# Optional: raises GitHub REST API rate limits for public repository analysis.
# Create a fine-scoped token with public repository read access if needed.
GITHUB_TOKEN=

# Repository scan guardrails.
MAX_REPOSITORY_TREE_ITEMS=3000
MAX_FETCH_FILE_BYTES=120000
